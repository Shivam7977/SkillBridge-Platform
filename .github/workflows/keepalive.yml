name: Keep Alive

on:
  schedule:
    - cron: "*/12 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping SkillBridge
        run: curl -f https://skillbridge-platform-bszo.onrender.com/health